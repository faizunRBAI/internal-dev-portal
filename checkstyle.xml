<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <module name="SuppressWarningsFilter"/>

    <!-- LineLength at Checker level (FileSetCheck, not TreeWalker) -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
    </module>

    <!-- Suppress checks inside test directories -->
    <module name="SuppressionSingleFilter">
        <property name="files" value="[\\/]test[\\/]"/>
        <property name="checks" value="MagicNumber|JavadocMethod|JavadocVariable|LineLength"/>
    </module>

    <!-- HomeController contains minified HTML/CSS/JS inside text blocks —
         long lines are intentional and not real Java code lines -->
    <module name="SuppressionSingleFilter">
        <property name="files" value="HomeController\.java"/>
        <property name="checks" value="LineLength"/>
    </module>

    <module name="TreeWalker">
        <module name="SuppressWarningsHolder"/>

        <!-- ── Imports ────────────────────────────────────────── -->
        <module name="UnusedImports"/>
        <module name="IllegalImport"/>
        <module name="RedundantImport"/>

        <!-- ── Naming conventions ────────────────────────────── -->
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="ParameterName"/>
        <module name="PackageName">
            <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
        </module>

        <!-- ── Best practices ────────────────────────────────── -->
        <module name="NeedBraces">
            <property name="allowSingleLineStatement" value="true"/>
        </module>
        <module name="EqualsHashCode"/>
        <module name="SimplifyBooleanReturn"/>
        <module name="StringLiteralEquality"/>
        <module name="EmptyCatchBlock">
            <property name="exceptionVariableName" value="ignored|expected"/>
        </module>
    </module>
</module>
